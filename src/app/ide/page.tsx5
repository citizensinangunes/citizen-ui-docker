"use client";

import { useEffect } from 'react';
import { useRouter } from 'next/navigation';

export default function IDEPage() {
  const router = useRouter();
  
  useEffect(() => {
    // Redirect to the sites page since IDE has been removed
    router.push('/sites');
  }, [router]);
  
  return null;
}